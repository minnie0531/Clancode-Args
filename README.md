# This repository is to understand clean code Chaper 14

1. Check Boolean parameters and related functions
2. Apply the changes to String and Integer parameters and functions.
3. The order of folder is
    Args-Boolean -> Args-Draft -> Args-AddTest -> Args-FirstRefactoring -> Args
4. Move from Args to Args1 -> Args2 -> Args3 with increment by 1.
